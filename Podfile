# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OCRCode' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OCRCode

  target 'OCRCodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OCRCodeUITests' do
    # Pods for testing
  end

end
